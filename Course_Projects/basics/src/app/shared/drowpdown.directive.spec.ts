import { DrowpdownDirective } from './drowpdown.directive';

describe('DrowpdownDirective', () => {
  it('should create an instance', () => {
    const directive = new DrowpdownDirective();
    expect(directive).toBeTruthy();
  });
});
